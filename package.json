{
  "name": "geoly-cli",
  "version": "0.2.2",
  "private": true,
  "description": "GEOly CLI — the GEOly command-line interface, built for agents. https://www.geoly.ai",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/bin.ts",
    "typecheck": "tsc --noEmit",
    "verify:retry": "tsx scripts/verify-retry.mts",
    "verify:org-picker": "tsx scripts/verify-org-picker.mts",
    "verify:picker": "tsx scripts/verify-picker-keys.mts"
  },
  "dependencies": {
    "clipanion": "4.0.0-rc.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
